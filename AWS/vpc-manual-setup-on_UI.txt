vpc>subnets(public/private(attach to a vpc))>igw((attach to a vpc))>NAT gateway(must be resided in public subnet, then allocate EIP)>create private and public Route tables(then we need to associate public/private subnets with respective route tables in the subnet association field)> opt the igw in the Route field in the Public Route Table > opt the NAT gateway in the Route files in the Private Route Table